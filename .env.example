# 复制为 .env.local 后，使用：docker compose --env-file .env.local up -d --build
# 填浏览器可访问的商城地址。手机扫码需使用 Cloudflare Tunnel 等公网 HTTPS 地址；本机可用 localhost。
ORDERFLOW_MOCK_PAYMENT_PUBLIC_BASE_URL=https://your-public-domain.example
